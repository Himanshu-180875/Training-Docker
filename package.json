{
  "name": "Projects",
  "private":"true",
  "version": "1.0.0",
  "dependencies": {
    "lerna": "^5.1.6"
  },
  "scripts": {
    "start": "lerna run start --stream",
    "test": "lerna run test",
    "new-version":"lerna version --conventional-commits --yes",
    "diff":"lerna diff"
  }
  
}
